/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com"],
    domains: ["217.196.49.184", "via.placeholder.com"],
  },
  reactStrictMode: false,
};

export default nextConfig;
